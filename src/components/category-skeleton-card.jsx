import { Skeleton } from '@chakra-ui/react'
import React from 'react'

const CategorySkeletonCard = () => {
  return (
    <Skeleton rounded={'lg'} w={'full'} h={'300px'}/>
  )
}

export default CategorySkeletonCard